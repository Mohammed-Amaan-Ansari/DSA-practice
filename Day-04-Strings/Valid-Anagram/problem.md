#  Valid Anagram

## Problem Statement
Given two strings s and t, return true if t is an anagram of s.

An anagram contains same characters with same frequencies.

---

## Example
Input: s = "anagram", t = "nagaram"
Output: true

Input: s = "rat", t = "car"
Output: false